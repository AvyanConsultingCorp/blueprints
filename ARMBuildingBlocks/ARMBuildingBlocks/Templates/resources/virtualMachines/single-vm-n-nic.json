{
  "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "virtualMachinesSettings": {
      "type": "object",
      "metadata": {
        "description": "Settings for virtual machines."
      }
    }
  },
  "variables": {
    "location": "[resourceGroup().location]",
    "templateToInvoke": [
      "[uri(deployment().properties.templateLink.uri, concat('single-vm-n-nic-no-avset.json'))]",
      "[uri(deployment().properties.templateLink.uri, concat('single-vm-n-nic-avset.json'))]"
    ]
  },
  "resources": [
    {
      "type": "Microsoft.Resources/deployments",
      "apiVersion": "2015-01-01",
      "name": "[concat('deploy-', variables('templateToInvoke'))]",
      "properties": {
        "mode": "Incremental",
        "templateLink": { "uri": "[variables('templateToInvoke')[mod(add(length(parameters('virtualMachinesSettings').availabilitySetName), 2), add(length(parameters('virtualMachinesSettings').availabilitySetName), 1))]]" },
        "parameters": {
          "virtualMachinesSettings": { "value": "[parameters('virtualMachinesSettings')]" }
        }
      }
    }
  ]
}